name: NSLAB matrix gold 20180628 20260715T222908 retired

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  actions: write

concurrency:
  group: nslab-matrix-gold-20180628-20260715T222908-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  stop:
    if: github.head_ref == 'nslab-matrix-20180628-20260715t222908'
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Superseded by current-session local semantic execution.'
