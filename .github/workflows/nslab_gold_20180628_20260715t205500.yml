name: NSLAB gold 20180628 20260715T205500

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  actions: read
  models: read

concurrency:
  group: nslab-gold-20180628-20260715T205500-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  execute:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    steps:
      - name: Superseded execution disabled
        run: echo superseded
