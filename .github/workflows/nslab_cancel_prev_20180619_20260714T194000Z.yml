name: NSLAB cancel previous execution 20180619 20260714T194000Z

on:
  pull_request:
    types: [synchronize]

concurrency:
  group: nslab-exec-20180619-20260714T194000Z-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  cancel:
    if: github.head_ref == 'nslab-exec-20180619-20260714T194000Z'
    runs-on: ubuntu-latest
    steps:
      - run: echo previous-execution-cancelled-by-current-run
