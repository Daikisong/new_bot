name: NSLAB blind snapshot 20180611 20260714t044918z

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  acquire_blind:
    if: github.head_ref == 'nslab-acquire-20180611-20260714t044918z'
    runs-on: ubuntu-latest
    steps:
      - name: Fetch pre-seal blind snapshot Raw bytes only
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p acquired_blind
          node <<'NODE'
          const fs = require('fs');
          const crypto = require('crypto');
          const url = 'https://raw.githubusercontent.com/Daikisong/stock-web/refs/heads/main/atlas/research_daily/snapshots/2018/06/20180608.csv';
          const path = 'acquired_blind/blind_snapshot_20180608_tmp.csv';
          (async () => {
            const response = await fetch(url, {
              redirect: 'follow',
              headers: {
                'cache-control': 'no-cache',
                'pragma': 'no-cache',
                'user-agent': 'nslab-gold-phase-machine/20180611-blind'
              }
            });
            if (!response.ok) throw new Error(`${url}: HTTP ${response.status}`);
            const bytes = Buffer.from(await response.arrayBuffer());
            fs.writeFileSync(path, bytes);
            const manifest = {
              phase: 'PRESEAL_BLIND_ONLY',
              url,
              path,
              byte_size: bytes.length,
              sha256: crypto.createHash('sha256').update(bytes).digest('hex'),
              first_line: bytes.toString('utf8').split(/\r?\n/, 1)[0]
            };
            fs.writeFileSync('acquired_blind/blind_acquisition_manifest.json', JSON.stringify(manifest, null, 2) + '\n');
          })().catch((error) => {
            console.error(error);
            process.exit(1);
          });
          NODE

      - name: Upload freshly acquired blind snapshot
        uses: actions/upload-artifact@v4
        with:
          name: nslab-blind-20180611-20260714t044918z
          path: acquired_blind/
          if-no-files-found: error
          retention-days: 1
