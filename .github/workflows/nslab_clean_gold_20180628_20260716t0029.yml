name: NSLAB isolated gold 20180628 20260716T0029 retired

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

concurrency:
  group: nslab-isolated-gold-20180628-20260716T0029-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  stop:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo retired
