name: NSLAB current-run fresh full-row review 20220824 20260715T045906

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  actions: read

concurrency:
  group: nslab-currentrun-20220824-20260715T045906-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  supersede:
    if: github.head_ref == 'nslab-currentrun-20220824-20260715t045906kst'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Supersede retained model-heavy attempt
        run: echo 'The prepared acquisition artifact is retained; semantic work moved to 28-row current-run micro jobs.'
